{
  "name": "@pcc/shared",
  "version": "1.0.0",
  "module": "dist/index.js",
  "types": "index.ts",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc index.ts --declaration --allowJs --outDir dist"
  },
  "dependencies": {
    "@pcc/shared": "file:"
  }
}
